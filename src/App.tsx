import React from 'react';
import CountryListItemPage from './pages/CountryListItemPage';

const App : React.FC = () => {
  return (
    <React.Fragment>
      <CountryListItemPage />
    </React.Fragment>
  );
};

export default App;
